# This file configures the 2-security stage for the non-production environment.
factories_config:
  # This stage uses data files directly, not a dataset.
  keyrings: data/keyrings
  cas: data/certificate-authorities
